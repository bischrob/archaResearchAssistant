# Interaction Log

Purpose: append a brief record of each user interaction and response status.

## 2026-03-16
- 2026-03-16 16:37:04 UTC | User asked: "Make sure to log every interactio. Have all OCR/text files been pulled from SOL?" | Agent action: checked local OCR counts/timestamps and SOL-related logs; added this log note and linked it in `Vault/Index.md`.
- 2026-03-16 16:40:55 UTC | User asked: "What 3 pdfs in the /mnt/pooled/Media/Books/pdfs mount (accessed over samba, not local) are not found in the ocr text?" | Agent action: diffed mounted PDF basenames (4304) vs OCR text basenames (4298); found 11 missing OCR matches and returned 3 full paths.
- 2026-03-16 17:23:57 UTC | User asked: "run paddle ocr on these three files" (with follow-up to check existing environments first) | Agent action: audited local conda envs, aligned Paddle stack to project pin (`paddlepaddle==3.2.0`, `paddleocr[doc-parser]==3.3.3`), completed OCR for `baggio...` (6 pages) and `brughmans...` (empty PDF), and started checkpointed run for `duff...` (observed progress 10/255 pages).
- 2026-03-16 18:07:47 UTC | User asked: "Is this all completed?" | Agent action: checked active process and checkpoint summary; confirmed `duff...` OCR is still running with progress at 150/255 pages.
- 2026-03-16 18:14:25 UTC | User asked: "What's the current status?" | Agent action: checked live process, checkpoint summary, and both `duff` text outputs; `duff` run remains active at 170/255 pages.
- 2026-03-16 18:53:11 UTC | User asked: "status update" | Agent action: checked process + summary; confirmed `duff...` OCR completed successfully (`status=ok`, 255/255 pages, 11848 lines).
- 2026-03-16 19:04:27 UTC | User asked to add 250 new Qwen3 reference-split training examples from OCR text with no duplicates. | Agent action: generated `reference_split_local_plus250_from_ocr_20260316.jsonl` (250 rows), audited zero overlap/duplicates against existing split datasets, and produced merged `reference_split_local_plus381_20260316.jsonl`.
- 2026-03-16 19:08:27 UTC | User asked: "How many do we have total now?" | Agent action: counted rows in the new (+250), base (+131), and merged (+381) datasets and reported totals.
- 2026-03-16 19:15:04 UTC | User asked to add +119 examples to reach 500, then submit Qwen3 training on SOL with sbatch and document process. | Agent action: generated `+119` (then `+2` to compensate dedup collisions), built final `reference_split_local_500_20260317.jsonl` and `train/eval` split (450/50), synced artifacts to `/scratch/rjbischo/researchAssistant`, submitted `scripts/sbatch_sol_qwen3_reference_split_dataset.sh`, and captured SOL job `49004815` (pending priority).
- 2026-03-16 19:55:12 UTC | User asked: "what's the current status?" | Agent action: queried SOL queue/accounting and slurm logs for job `49004815`; job remains `PENDING` with reason `(Priority)` and no slurm output/error content yet.
- 2026-03-16 20:45:13 UTC | User asked: "status?" | Agent action: refreshed SOL queue/accounting and log check for job `49004815`; still `PENDING` with reason `(Priority)`, no slurm output/error content yet.
- 2026-03-16 20:47:38 UTC | User asked: "What resources are being requested?" | Agent action: inspected `scripts/sbatch_sol_qwen3_reference_split_dataset.sh` and queried `scontrol show job 49004815` to report requested CPU, memory, GPU, partition, QoS, and walltime.
- 2026-03-16 20:54:09 UTC | User asked to switch to CPU and request minimum needed resources. | Agent action: cancelled pending GPU job, added `scripts/sbatch_sol_qwen3_reference_split_dataset_cpu.sh` (CPU-only minimal profile), synced to SOL, and submitted job `49006992` (`cpu=4`, `mem=24G`, `gpu=0`), which entered `RUNNING`.
- 2026-03-16 22:58:52 UTC | User asked: "status?" | Agent action: checked SOL queue/accounting and logs for CPU job `49006992`; confirmed `RUNNING` with elapsed ~2:06 and training progress around `11/57` steps in epoch (slow but advancing).
- 2026-03-17 02:31:50 UTC | User asked: "check the results" | Agent action: inspected job `49006992`, slurm logs, and output model directory; training is still running (~36/57 steps seen), and no final artifacts/metrics are written yet because save/eval strategy is epoch-boundary.

## 2026-03-18
- 2026-03-18 20:59:41 UTC | User asked to check Vault and investigate Zotero 8 plugin install failure using `zoterodebuglog.txt`. | Agent action: verified Vault index state, analyzed debug log and plugin build/install scripts, confirmed session was in Safe Mode (`safeMode => true`) with no `rag-sync` load entries, validated XPI build success, and added troubleshooting notes to Vault.
- 2026-03-18 21:03:30 UTC | User provided updated `zoterodebuglog.txt` with new install output. | Agent action: identified definitive error `applications.zotero.update_url not provided` causing `Invalid XPI`, patched plugin manifest to include `applications.zotero.update_url`, hardened XPI build checks, rebuilt XPI, and updated troubleshooting notes.
- 2026-03-18 21:07:07 UTC | User provided another updated `zoterodebuglog.txt` after patch. | Agent action: confirmed the same `applications.zotero.update_url not provided` + `Invalid XPI` error persists in Zotero logs, verified repo and built XPI already contain `update_url`, and documented that Zotero is likely loading a stale profile extension copy requiring remove/reinstall.
- 2026-03-18 21:08:41 UTC | User requested auto-replacing extension XPI after each plugin update. | Agent action: updated `scripts/build_zotero_plugin_xpi.sh` to auto-run `scripts/install_zotero_plugin_windows.sh` after successful build by default (`INSTALL_AFTER_BUILD=1`), documented opt-out (`INSTALL_AFTER_BUILD=0`), and updated shell script notes in Vault.
- 2026-03-18 21:10:50 UTC | User reported build/install error: `Could not locate Zotero profiles.ini` and `No profile sections found in /profiles.ini`. | Agent action: patched installer path discovery and error handling, added WSL profile-base scanning (`/mnt/<drive>/Users/*/AppData/Roaming/Zotero/Zotero`), guarded against empty profile base, and validated install logic with a temporary `profiles.ini` integration test.
- 2026-03-18 21:20:03 UTC | User reported plugin installed but only `Print` appeared under Tools. | Agent action: traced behavior to placeholder menu registration using `l10nID: menu-print`, replaced it with explicit `RAG Sync` submenu injection (`Sync Now`, `Retry Failed`, `Pause/Resume`, `Show Diagnostics`), rebuilt XPI, and updated troubleshooting notes.
- 2026-03-18 22:06:38 UTC | User reported `Sync Now` shows no visible action and requested error notification behavior. | Agent action: implemented visible progress + error alerts for `Sync Now` (`Zotero.ProgressWindow` + `Zotero.alert`), added running-state menu label update, set plugin pref defaults in `prefs.js`, rebuilt/reinstalled XPI, and updated troubleshooting notes.
- 2026-03-18 22:23:20 UTC | User asked whether plugin-triggered sync started/finished/errored/running. | Agent action: queried runtime endpoints and server logs; confirmed `POST /api/sync` reached backend (HTTP 200), sync job transitioned to `failed` immediately with error `PDF source directory not found: \\\\192.168.0.37\\pooled\\media\\Books\\pdfs`, ingest remained `idle`.
- 2026-03-18 22:25:21 UTC | User requested more informative plugin sync feedback with loading indicator and exact progress updates (e.g., filenames). | Agent action: updated backend `/api/sync` progress reporting to emit per-file messages (`Scanning i/n: <filename>`), updated plugin `Sync Now` to poll `/api/sync/status` until terminal state, show live progress/message updates, capture success summary/error detail, rebuilt and reinstalled plugin XPI, and documented behavior in Vault notes.
- 2026-03-18 22:30:52 UTC | User requested centered sync notification UI and configurable linked-files source directory (`C:\Users\rjbischo\Nextcloud\zotero`). | Agent action: replaced popup progress window with centered in-app overlay, added plugin pref `extensions.zotero-rag-sync.sourceDir`, added `/api/sync` support for request `source_dir`, added WSL Windows-drive path resolution (`C:\...` -> `/mnt/c/...`), rebuilt/reinstalled plugin, and verified sync status accepted the new source path.
- 2026-03-18 22:30:52 UTC | User clarified Zotero stores linked PDFs inside ZIP files and requested support. | Agent action: added ZIP-aware source collection/extraction cache for sync/ingest candidate discovery, wired sync progress updates for ZIP inspection and per-file scan messages, and validated backend accepts `source_dir=C:\Users\rjbischo\Nextcloud\zotero` with ZIP scanning in progress.
- 2026-03-18 22:44:35 UTC | User asked how many PDFs the Zotero library says there should be. | Agent action: queried `/mnt/c/Users/rjbischo/Zotero/zotero.sqlite` directly and reported non-deleted PDF attachment counts: total `3882`, My Library (`libraryID=1`) `3710`, groups `libraryID=2` `138`, `libraryID=3` `34`.
- 2026-03-18 22:51:28 UTC | User asked to update plugin/backend to use attachment paths as stored in Zotero DB and asked about reusing Zotero WebDAV link. | Agent action: added sync `source_mode` (`zotero_db`/`filesystem`), wired plugin default to `zotero_db`, added Zotero 8 schema-compatible creator query fallback (`creators.firstName/lastName`), added automatic `zotero.sqlite` path discovery fallback, rebuilt/reinstalled plugin, and validated DB-path sync progress resolves attachment paths with live filename updates.
- 2026-03-18 22:57:49 UTC | User asked whether Zotero-reported sync is currently running. | Agent action: queried `/api/sync/status` and confirmed active run (`status=running`, request `8605bc52-f527-4d1e-8ce0-06e98f6c9f1f`) with live progress in Zotero-path resolution phase.
- 2026-03-18 23:07:19 UTC | User requested combined sync+ingest behavior plus SOL training-run reconciliation and LoRA workflow update. | Agent action: updated `/api/sync` to support combined sync+ingest (`run_ingest` default true), updated plugin sync payload/summary accordingly, added DB-backed source mode defaults in plugin/backend, pulled missing SOL run `qwen3-reference-split-500-cpu_49006992` into local `models/`, and updated settings default to auto-select newest local Qwen citation adapter when env override is absent.
- 2026-03-18 23:39:40 UTC | User requested stronger duplicate handling and a different 10-file sync run. | Agent action: hardened dedupe key normalization (accent/diacritic-safe title-year keys), added normalized title+year identity extraction from Neo4j, updated duplicate checks to use both canonical and normalized identity sets, fixed custom ingest selection to avoid full-source scan in custom mode, hardened Zotero DB reads with `immutable=1` fallback, ran targeted 10-file sync/ingest against a different Zotero storage slice (10 ingested, 0 failed), and documented details in `60_Troubleshooting/07_duplicate_detection_hardening_2026-03-18.md`.

## 2026-03-19
- 2026-03-19 00:11:20 UTC | User reported RAG Sync showing `idle` but notification/overlay could not be closed. | Agent action: patched plugin sync UI to add a manual `Close` button on the centered overlay, treated backend `idle` as a terminal sync state, and prevented stale overlay updates after manual dismiss; rebuilt and reinstalled `rag-sync@rjbischo.local.xpi`.
- 2026-03-19 00:17:40 UTC | User asked to fix missing-ingest behavior for a specific Zotero paper but run ingest manually. | Agent action: diagnosed metadata-filter exclusion due API runtime lacking `ZOTERO_DB_PATH`/`ZOTERO_STORAGE_ROOT`, implemented Zotero DB auto-discovery and storage-root fallback in metadata loader, removed forced Paperpile fallback when `zotero_db_path` is empty (for `metadata_backend=zotero`), updated API/pipeline metadata-index gating accordingly, and verified the target PDF is now selectable for ingest without manual env overrides.
- 2026-03-19 00:24:55 UTC | User requested sync UI background/cancel controls, cancellation of current run, preprocessing capped to 10% progress, and backend restart. | Agent action: cancelled active sync (`request_id=cd07fbe0-205f-4c5d-b067-4ec997339604`), added plugin overlay controls (`Run in Background`, `Cancel Sync` -> `/api/sync/stop`), remapped backend sync progress so preprocessing stages stay within 0–10% before scan/ingest, rebuilt/reinstalled plugin XPI, restarted backend, and verified `/api/sync/status=idle` and `/api/health=ok`.
- 2026-03-19 01:08:30 UTC | User requested implementation of a full codebase/workflow review plan with parallel sub-review tracks and Vault-only documentation output. | Agent action: executed read-first parallel review across backend data path, API/plugin lifecycle, ops/workflow scripts, and testing reliability; synthesized findings into one decision-ready master risk/action note plus four subsystem review notes; added source-of-truth docs-drift table, milestone action plan (M0/M1/M2), owners, acceptance criteria, regression-test expectations, and rollout checkpoints; linked all notes in `Vault/Index.md` and appended this interaction log entry.
- 2026-03-19 03:39:40 UTC | User asked: "Write up a doc for the api." | Agent action: authored `Vault/20_WebAPI/08_api_reference.md` with current endpoint/auth/job-lifecycle reference from `webapp/main.py`, added usage examples, and linked it in `Vault/Index.md` and `Vault/20_WebAPI/01_api_surface.md`.
- 2026-03-19 04:10:00 UTC | User asked to submit Qwen3 section/reference accuracy evaluation as an sbatch process with SOL best-practice checks and minimal resources. | Agent action: reviewed ASU RC SOL Slurm docs (resource requests, sbatch hygiene, transition guidance), added `scripts/eval_qwen_reference_adapter_accuracy.py` (adapter-only split+section metrics), added `scripts/sbatch_sol_qwen3_reference_eval.sh` with conservative defaults (`-c 4`, `--mem=16G`, `-G 1g.20gb:1`, `-t 4h`, `--export=NONE`), and prepared direct submit commands; SSH submission from this environment was blocked by DNS resolution failure for SOL hostnames.
- 2026-03-19 04:28:00 UTC | User asked for a documentation review due LLM agents struggling, specifically to document citation-only lookup for known papers. | Agent action: reviewed root README + Vault API docs for task-flow clarity; added citation-first known-paper workflows and examples (`/api/article/{citekey}`, `/api/articles/by-citekeys`, paper-scope `/api/query` with `chunks_per_paper=1`) to `README.md` and `Vault/20_WebAPI/08_api_reference.md`; added dedicated quickstart `Vault/20_WebAPI/09_citation_lookup_quickstart.md`; updated Vault navigation links in `Vault/Index.md` and `Vault/README.md`.
- 2026-03-19 04:45:00 UTC | User asked for a broader LLM-focused documentation review and commit. | Agent action: audited project/Vault docs for drift and agent-ambiguity; updated stale notes (`00_Project/01`, `03`, `04`, `05`, `40_Scripts/03`) to match current Zotero-first and hybrid parser behavior; added `00_Project/12_llm_agent_playbook.md` with source-of-truth order, task recipes, and failure-mode guidance; linked new note in `Vault/Index.md` and `Vault/README.md`; then committed documentation changes.
